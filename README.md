# SingBoxTunGui-Encrypted-Windows-Distribution
Packages a PowerShell/WinForms app as a signed Windows executable. The build pipeline encrypts the script with AES-256, embeds it in a C# launcher, and ships checksums and a public certificate for safer, simpler distribution.
